name: quakesafe
description: A new Flutter project for earthquake safety.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0
  vibration: ^1.9.0
  torch_light: ^1.0.0
  geolocator: ^10.1.0
  lucide_icons: ^0.300.0
  url_launcher: ^6.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
